# Disabled for manual deployment - uncomment when ready for auto-deployment
# name: Deploy to Vercel and Render
# 
# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]
# 
# jobs:
#   deploy-frontend:
#     name: Deploy Frontend to Vercel
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       
#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'
#           cache: 'npm'
#           cache-dependency-path: frontend/package-lock.json
#       
#       - name: Install dependencies
#         run: |
#           cd frontend
#           npm ci
#       
#       - name: Build project
#         run: |
#           cd frontend
#           npm run build
#         env:
#           NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
#           NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}
#       
#       - name: Deploy to Vercel
#         uses: amondnet/vercel-action@v20
#         with:
#           vercel-token: ${{ secrets.VERCEL_TOKEN }}
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           vercel-args: '--prod'
#           vercel-org-id: ${{ secrets.ORG_ID }}
#           vercel-project-id: ${{ secrets.PROJECT_ID }}
#           working-directory: frontend
# 
#   deploy-backend:
#     name: Backend Ready for Render
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       
#       - name: Setup Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.11'
#       
#       - name: Install dependencies
#         run: |
#           cd backend
#           pip install -r requirements.txt
#       
#       - name: Run tests
#         run: |
#           cd backend
#           # Add your test commands here
#           python -c "import main; print('Backend imports successfully')"
#       
#       - name: Notify Render deployment ready
#         run: |
#           echo "Backend is ready for deployment on Render"
#           echo "Make sure to set environment variables in Render dashboard" 